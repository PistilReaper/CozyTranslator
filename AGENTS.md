# 界面约定

- 文案只保留操作名称、必要设置说明和状态反馈。标题下不加宣传口号、装饰性介绍或重复中英标签。
- 保持暖色、克制的排版；用留白和层级组织内容，不靠增加介绍文字填满页面。
- 主窗口默认显示在任务栏；失焦不自动隐藏。隐藏到托盘必须由用户主动触发。
- 保留 Windows 原生拖拽、缩放、最小化行为。切换翻译和查词不得重置用户调整的窗口大小。
- 改动窗口边框或生命周期时，除自动检查外，运行 `tools/window-check.ps1` 对测试进程做实际鼠标验收。不要只用程序设置 Width/Height 证明可缩放。
- Cozy 与 Translator 使用同一字号、字重和颜色；英文也使用无衬线字体。默认正文 18，界面标签 14，并提供全局字号设置。
- 模式切换区按内容自动计算高度，不用写死的行高裁切文字。空闲状态不显示宣传文案或模型标语。
- 空白区域可拖动窗口，输入和选择文本不触发窗口拖动。
- 弹出菜单属于独立视觉树，不能被窗口拖拽事件拦截。下拉选项需用真实鼠标验收，直接设置 SelectedIndex 不能代替点击验证。
- 模式 1 在外部复制后自动翻译，不抢焦点、不擅自恢复隐藏窗口；模式 2 是显式选区手势，展示结果但保持原文焦点。设置编辑和仅手动模式不自动查询；模式互斥。
- 交付前核对用户实际运行的 exe 路径和版本，更新正在使用的目录，避免新压缩包与旧启动入口脱节。

- 译文支持轻量 Markdown 和本地公式排版；复制保留原始 Markdown/LaTeX。流式更新复用已排版公式，测试真实文本选择、空白拖动、主题、字号和复制，不引入浏览器内核。

- 发布公测版时标记 GitHub Pre-release；源码与发行包不得包含本地设置、API Key、SDK 工具或个人构建路径。
