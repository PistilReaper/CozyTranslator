# CozyTranslator v0.3.0-beta.1 · 验证记录

日期：2026-09-16。环境：Windows 11 24H2 x64；.NET SDK 10.0.401 / Runtime 10.0.12。

## 已验证

本轮新增右键选区模式。独立文档进程接受实际鼠标输入：左键按住时点右键，单词进入词典、句子进入翻译；检查原应用右键菜单、焦点及剪贴板不被改动，其他模式和设置编辑期间不触发。新增配置保存与旧版暂停状态迁移测试。

- **42 项核心测试**：自动模式、标点及词内连字符、提示词五档与方向、词典结构、三种协议、鉴权、URL、UTF-8 分片、原生流事件、推理内容过滤、非流查词、流中断、旧请求隔离及停止保留部分内容。
- **61 项 Windows 检查**：DPAPI 加密往返、快捷键冲突及非法值、本地英文 SAPI、文本输入与结果绑定、查词切换、重译去重、错误及停止反馈、未识别词条、最小尺寸操作可见；任务栏样式与图标、原生标题和缩放样式、标题空白命中、八向缩放命中、边缘真实指针命中、失焦保持显示、查词进入及退出保留用户尺寸；默认字号与无衬线字体、模式高度、大字号布局、外观实时预览和无 Key 保存、连续注册表亮暗变化及手动主题覆盖；真实外部进程复制自动翻译、自身复制防循环、相同文本去重、暂停与设置期间停用、复制单词查词。
- **13 项实际发布程序窗口交互**：鼠标拖动标题、侧边留白和译文空白处，编辑器拖拽不移动窗口，右边缘横向缩放、底边缘纵向缩放、任务栏样式、Windows 任务栏实际运行按钮、标题设置按钮、最小化及恢复、隐藏到托盘及恢复、Esc 悬浮球与单实例恢复、原生关闭时隐藏到托盘。记录见 `artifacts/qa/window-interactions-v0.3.0-beta.1.txt`。
- **3 项主题真实鼠标检查**：分别点击深色、浅色、跟随系统，核对下拉选中值及窗口实际背景像素；深色 `#242522`，浅色 `#F8F6F1`。记录见 `artifacts/qa/theme-clicks-v0.3.0-beta.1.txt`。
- 实际 WPF 渲染检查：浅色、深色、查词、长文本、加载、停止、错误、设置、最小窗口和悬浮球。已修复图标字体覆盖、下拉框深色样式、窗口最小尺寸裁切。
- Release 编译与 Windows x64 自包含发布成功。仅全局安装了 .NET 8 的本机已直接运行包含 .NET 10 的发布包，未要求另装桌面运行时。

v0.2 已加入原生 Markdown 与公式译文视图。新增检查覆盖流式显示、标题/列表/表格、四种公式定界符、中文邻接、分数/积分/求和/矩阵及 `align`、代码与货币不误识别、不完整公式、公式复制、实际剪贴板、主题和字号、超长公式横向滚动、长文本增量更新及保持滚动位置。真实鼠标验证了富文本选择不移动窗口、译文空白处仍可拖动。

Markdown 实测样本为 约 4,000 字符、75 个公式：首次排版约 0.8–0.9 秒；复用公式后追加文字的渲染约 25–50 ms（不含网络等待；单机测试，非通用性能保证）。流式输出合并到 120 ms 一次更新。排版完全在本地完成，应用不启动浏览器内核。

截图见 `artifacts/qa/markdown-light.png`、`markdown-dark.png`、`markdown-narrow.png`。示例为测试夹具，不是实际 DeepSeek 调用结果。

移除了标题口号、重复中英标签和风格描述；设置页只保留字段及必要说明。后续文案约定已写入项目 `AGENTS.md`。

测试数据只存在于测试项目。用户应用没有演示结果或自动填写的真实密钥。

## 性能

以下是 v0.1 的历史基线，v0.3.0-beta.1 本轮未重测性能。在 v0.1 实际发布程序完成设置页与窗口操作、收起到悬浮球后，采样 60 秒：

| 指标 | 实测 |
|---|---|
| 私有内存 | 107.8 MiB |
| 工作集 | 168.6 MiB |
| 平均 CPU（按 32 个逻辑处理器归一） | 0.000%（测量精度内） |
| 进程首次进入输入空闲 | 443 ms；此指标不等同于首帧绘制时间 |

数据来自 `artifacts/qa/performance.json`。实际内存会随窗口、语音和文本长度变化，网络响应时间取决于服务商与模型。安装包大小由包含的自包含运行时决定。

## 验证边界

- 右键模式通过 Windows UI Automation 读取当前选区；独立 WPF 文档进程已验收，尚未逐一认证所有 Word、PDF、浏览器版本。扫描图片、不提供 TextPattern 的控件和高权限窗口不保证支持；没有读到选区不调用模型。
- LaTeX 为 WPF-Math 支持的轻量子集：已测 `pmatrix`、`align`；`aligned`、自定义宏和完整 TeX 文档不是本轮支持范围。未闭合或未支持公式保留源文，复制仍保留原始输出。

- 本轮未调用 DeepSeek、Claude、Gemini 的实际账户；协议回归使用 HTTP 响应夹具。用户可在设置内点“测试连接”。
- 浅深色调色板及 WPF 控件切换已经渲染验证；注册表通知测试使用临时测试键，验证连续的深浅变化和手动覆盖，没有改动用户的 Windows 全局主题设置。
- 支持按显示器工作区约束位置与尺寸，但尚未在不同物理显示器、不同 DPI 的组合上逐一验收。
- SAPI 需正常桌面用户权限。受限测试沙箱无法访问语音组件，在正常 Windows 权限下验证通过；应用对拒绝访问提供提示。
- 当前交付是 ZIP 便携包，没有代码签名证书或安装向导。

## 重现

```powershell
dotnet run --project tests/CozyTranslator.Tests -c Release
dotnet run --project tests/CozyTranslator.WindowsTests -c Release -- artifacts/qa
dotnet publish src/CozyTranslator.App -c Release -r win-x64 --self-contained true -o artifacts/CozyTranslator-v0.3.0-beta.1-win-x64
```

`tools/theme-click-check.ps1 -AppProcessId <测试进程 ID>` 验证主题的真实点击与实际窗口颜色，并恢复原选择；不保存设置。

`tools/window-check.ps1 -AppProcessId <测试进程 ID>` 对指定的测试应用执行真实鼠标及 UI Automation 检查，会短暂移动鼠标和测试窗口，结束后恢复鼠标、位置及尺寸，不调用模型或修改 API 配置。`tools/live-check.ps1` 可列出测试进程窗口。`tools/generate-design.py` 将最新测试渲染整理成两份独立 HTML 设计参考。Windows 测试会暂时写入已知测试剪贴板文本，并在结束时恢复原剪贴板。
